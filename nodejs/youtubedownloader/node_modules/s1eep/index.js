module.exports={
  asyncsleep:function (ms) {
    return new Promise((resolve) => {
      setTimeout(resolve, ms);
    });
  },
  sleep:function (ms){
    var start = new Date().getTime();
    var end = start;
    while(end < start + ms) {
      end = new Date().getTime();
   }
 }
}